---
import headerMenu from './headerMenuData'

const {} = Astro.props
---

<nav id="header-menu">
  <ul>
    {
      headerMenu.map((i) => (
        <li>
          <a href={i.url}>{i.title}</a>
        </li>
      ))
    }
  </ul>
</nav>
